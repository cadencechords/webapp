# Cadence web app

```bash
yarn install
yarn start        # dev server on http://localhost:3000
yarn test         # Jest
```

Screenshot catalog and smoke flows (Playwright): see [e2e/README.md](e2e/README.md).
